package Etapa3;

import java.util.ArrayList;
import java.util.List;

public class resumo_vetor {
    //Vetores:

    //criar: TIPO NOME[]=new TIPO[tamanho do vetor]- Tipo:int,String,double...

    //Preenchimento:
    //do{
    //   sout("Digite uma nota:");
    //   NOTAS[x]=tec.nextInt();
    //   x++;
    //}while(x<100);

    //lista dinamina:
    List<Integer>num=new ArrayList<>();
    //Tipo: Integer,Double,String,Float.

    //num.add(numero)=sempre vai adicionar o numero no fim da lista.

    //num.set(x,numero)=atualizar algum conteudo no indice x.

    //num.remove(x)=remove o que esta no indice x.
    //num.remove(num.size()-1)=remove o ultimo indice da lista.
    //num.clear()=remove todos os itens da lista;

    //num.get(x)=pega o conteudo que esta no indice x.

    //num.indexOf(parametro)=procura se existe o parametro pesquisado e retorna a posicao.

    //num.size()= retorna o tamanho da lista.

    //para imprimir uma lista dinamica:
    //sout(num.get(x));

    //Ordenação metodo bolha:
    //for(int aux=0;aux<vet.legth-1;aux++){
    //  for(int x=0;x<vet.legth-1;x++){
    //     if(lista.get(x)>lista.get(x+1){
    //        temp=lista.get(x);
    //        lista.set(x,lista.get(x+1);
    //        lista.set(x+1,temp);
}
